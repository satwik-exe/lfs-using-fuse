# Build artifacts
lfs
mkfs_lfs
src/*.o

# Disk image
lfs.img

# Mount point contents (never commit what's inside the mounted fs)
mount/*
!mount/.gitkeep

# Editor / OS junk
.DS_Store
*.swp
*.swo
*~

# Duplicate README in src/ (README belongs only in root)
src/README.md
